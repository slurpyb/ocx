{
	"name": "kit",
	"private": true,
	"scripts": {
		"build": "bun scripts/build.ts",
		"check": "bun run check:types",
		"check:types": "tsc --noEmit -p tsconfig.json",
		"deploy": "bun run build && wrangler deploy",
		"dev": "bun run build && wrangler dev --inspector-port 0"
	},
	"devDependencies": {
		"@slurpyb/ocx": "workspace:*",
		"wrangler": "^4.79.0"
	}
}
